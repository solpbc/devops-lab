# Journal — 2026-06-13

## What we did today

### 1. Connected the `solpbc` folder
Gave Claude access to `/Users/dvd/Documents/solpbc`, a local git repo for AMD SEV-SNP attestation tooling on Azure Confidential VMs. The repo bypasses Microsoft Azure Attestation (MAA) and instead roots trust directly in AMD silicon via the `AMD ARK → ASK/ASVK → VCEK/VLEK → SEV-SNP report` chain.

### 2. Explored the repo
Surveyed the layout: `Containerfile`, `run.sh`, `README.md`, and a PDF research brief (`docs/azure-sev-snp-attestation-brief.pdf`).

### 3. Created a `journal/` directory
Established this journaling convention — one Markdown file per session, named by date, committed to the repo.

---

## What we could do next

### Near-term (code & docs)
- **Read and review `run.sh`** — check for robustness, error handling, missing `set -euo pipefail`, etc.
- **Read and review `Containerfile`** — verify base image, layer ordering, minimal attack surface for a confidential-compute context.
- **Summarize the PDF brief** — extract key findings from `docs/azure-sev-snp-attestation-brief.pdf` into a readable Markdown doc.

### Medium-term (expand tooling)
- **Write a verification script** — a standalone script (Python or shell) that walks the full attestation chain: fetch HCL blob from vTPM NV index → extract AMD report → verify VCEK cert chain → verify vTPM quote.
- **Add a test harness** — mock or replay-based tests so the chain can be exercised without a live Confidential VM.
- **Document the PCR layout** — which PCRs cover what (UEFI, shim, kernel, initrd, IMA) and what policy to enforce.

### Longer-term (hardening & research)
- **IMA / dm-verity integration** — extend guest image integrity beyond launch measurement to runtime file access.
- **IETF RATS alignment** — map the current verification path to RFC 9334 Evidence/Endorsement/Appraisal terminology.
- **snpguest version tracking** — `snpguest --features hyperv` is a non-upstream feature; track upstream for eventual merge or divergence.
