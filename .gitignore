# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Rust / snpguest build artifacts
/target/
**/*.rs.bk
Cargo.lock

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Attestation artifacts & secrets (never commit)
*.pem
*.key
*.cert
*.crt
*.der
*.bin
*.quote
*.report
hcl-blob*
nonce*

# Pinned AMD CA roots used by the off-CVM verifier.
!roots/
!roots/amd/
!roots/amd/*/
!roots/amd/*/*.pem

# Logs & temp
*.log
*.tmp
tmp/

# ACI deployment scratch (params.json carries ACR credentials)
params.json
template.json

.demo-aci.env
aci-demo-out/
